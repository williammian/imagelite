import { Template } from '@/components'


export default function FormularioPage() {
    return(
        <Template>
            <h1 className="bg-gray-900 text-white">Formulário</h1>
        </Template>
    )
}